// Duolingo/CRED-style UI leans on noticeably larger radii than Tailwind's
// web defaults (which assume a mouse-precision cursor, not a thumb) — "xl"
// and "2xl" here are deliberately larger than Tailwind's stock scale.
export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  '2xl': 28,
  full: 9999,
};
